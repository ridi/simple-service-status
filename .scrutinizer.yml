checks:
  javascript: true
build:
  environment:
    mongodb: '3.2'
  tests:
    override:
      -
        command: 'npm test'
        coverage:
            file: 'coverage/clover.xml'
            format: 'clover'
      - 'npm run lint'
filter:
  excluded_paths:
    - tests/*
